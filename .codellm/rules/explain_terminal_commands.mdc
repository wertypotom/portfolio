---
description: * CRITICAL: Before every terminal command, provide a user-facing explanation in your response that includes:   1. What you're trying to accomplish   2. The exact command you'll run (in backticks)   3. What the command does and why it's needed    Example: "I'll check your Node.js version by running `node --version`. This command prints the currently active Node.js version number to verify the updat
globs: *
alwaysApply: true
---

# Your rule content

* CRITICAL: Before every terminal command, provide a user-facing explanation in your response that includes:
  1. What you're trying to accomplish
  2. The exact command you'll run (in backticks)
  3. What the command does and why it's needed
  
Example: "I'll check your Node.js version by running `node --version`. This command prints the currently active Node.js version number to verify the update was successful."

Do NOT rely solely on the tool's explanation parameter - that's internal metadata, not user communication.- You can use markdown but don't have to